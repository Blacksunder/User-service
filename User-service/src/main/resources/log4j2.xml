<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <!-- Файловый аппендер ТОЛЬКО для Hibernate -->
        <File name="HibernateFile" fileName="logs/hibernate.log" append="false">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </File>

        <!-- Консольный аппендер для вашего приложения (stdout) -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%msg%n"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- ВСЕ логи Hibernate идут только в файл -->
        <Logger name="org.hibernate" level="INFO" additivity="false">
            <AppenderRef ref="HibernateFile"/>
        </Logger>

        <Logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
            <AppenderRef ref="HibernateFile"/>
        </Logger>

        <Logger name="org.hibernate.type.descriptor.sql" level="TRACE" additivity="false">
            <AppenderRef ref="HibernateFile"/>
        </Logger>

<!--        &lt;!&ndash; Логи вашего приложения идут только в консоль &ndash;&gt;-->
<!--        <Logger name="" level="INFO" additivity="false">-->
<!--            <AppenderRef ref="Console"/>-->
<!--        </Logger>-->

        <!-- Корневой логгер для всего остального -->
        <Root level="WARN">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>